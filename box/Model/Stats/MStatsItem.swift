import UIKit

class MStatsItem
{
    let image:UIImage
    let title:String
    let count:Int
    
    init(image:UIImage, title:String, count:Int)
    {
        self.image = image
        self.title = title
        self.count = count
    }
}
