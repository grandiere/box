import Foundation

class MBoardsItem
{
    let number:Int
    let score:Int
    let name:String
    
    init(number:Int, score:Int, name:String)
    {
        self.number = number
        self.score = score
        self.name = name
    }
}
